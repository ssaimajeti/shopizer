#Spring boot configurations
#logging.config=log4j-shopizer.properties
server.port=8080

# Turn Statistics on (deprecated - Remove if not needed)
#spring.jpa.properties.hibernate.generate_statistics=true

# Default database schema
spring.jpa.properties.hibernate.default_schema=SALESMANAGER
#logging.level.org.hibernate.stat=debug

# Enable logging to verify that HikariCP is used, the second entry is specific to HikariCP
logging.level.org.hibernate.SQL=ERROR
logging.level.com.zaxxer.hikari.HikariConfig=INFO
# TRACE will print binding
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO

# When running from IDE (eclipse) or from springboot:run command
#server.contextPath=/shopizer (deprecated as of Spring Boot 2.0.0)

logging.level.org.springframework=ERROR
logging.level.com.shopizer=INFO
logging.level.org.hibernate=ERROR
logging.level.org.apache.http=ERROR

#logging.file=shopizer.log

# Default Spring white label error page
server.error.whitelabel.enabled=false

spring.servlet.multipart.max-file-size=4MB
spring.servlet.multipart.max-request-size=10MB

# Hibernate configuration (Ensure proper configuration for Hibernate 6 if migrating)
spring.jpa.hibernate.use-new-id-generator-mappings=true

# Spring boot 2.X configuration (deprecated - investigate necessity)
# spring.main.allow-bean-definition-overriding=true

# Updated actuator configurations for Spring Boot 3.x
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoint.health.show-components=always
management.health.probes.enabled=true
management.health.elasticsearch.enabled=false
management.health.mail.enabled=false
management.health.ping.enabled=true

# Build information (replace tokens with actual Maven build properties during build)
application-description=@project.description@
application-version=@project.version@
build.timestamp=@maven.build.timestamp@