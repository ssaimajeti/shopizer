### Task List

1. **Analyze Dependencies:** Review `mvn dependency:tree` for Maven (if applicable via CLI) to ensure all dependencies are compatible.
2. **Modify Code Imports:** Update all Java classes affected by package changes, especially `javax` to `jakarta`.